# DEPRECATED — this file is a leftover duplicate from before the nginx config
# was split into two topology-specific files. It is no longer referenced by
# the Dockerfile or docker-compose.yaml. Safe to delete.
#
# Use instead:
#   - standalone.conf  (nginx + php-fpm in one container — used by the Dockerfile)
#   - compose.conf     (nginx and php-fpm as separate containers — used by docker-compose.yaml)
